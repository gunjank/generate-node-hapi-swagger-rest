'use strict';

module.exports = function (server) {
    // read - If you want to divert index or home page call to swagger documentation
    // server.route({
    //     method: 'get',
    //     path: '/',
    //     handler: function (request, reply) {
    //         return reply.redirect('/documentation');
    //     }

    // });

}